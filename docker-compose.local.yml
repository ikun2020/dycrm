services:
  app:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    image: ${APP_IMAGE:-dycrm-local-app:latest}
    restart: unless-stopped
    working_dir: /var/www/html
    env_file:
      - .env.local
    ports:
      - "127.0.0.1:${APP_PORT:-3100}:80"
    volumes:
      - .:/var/www/html
      - vendor:/var/www/html/vendor
      - storage:/var/www/html/storage
      - bootstrap-cache:/var/www/html/bootstrap/cache
      - ./docker/nginx/local.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy

  mysql:
    image: mysql:8.4
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: ${DB_DATABASE:-dycrm}
      MYSQL_USER: ${DB_USERNAME:-dycrm}
      MYSQL_PASSWORD: ${DB_PASSWORD:-dycrm-local-password}
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-dycrm-local-root-password}
    volumes:
      - mysql-data:/var/lib/mysql
    ports:
      - "127.0.0.1:3307:3306"
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h127.0.0.1 -uroot -p\"$${MYSQL_ROOT_PASSWORD}\" --silent"]
      interval: 5s
      timeout: 5s
      retries: 30
      start_period: 20s

  redis:
    image: redis:7.4-alpine
    restart: unless-stopped
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - redis-data:/data
    ports:
      - "127.0.0.1:6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 20

  queue:
    image: ${APP_IMAGE:-dycrm-local-app:latest}
    restart: unless-stopped
    working_dir: /var/www/html
    env_file:
      - .env.local
    volumes:
      - .:/var/www/html
      - vendor:/var/www/html/vendor
      - storage:/var/www/html/storage
      - bootstrap-cache:/var/www/html/bootstrap/cache
    command: ["php", "artisan", "queue:work", "--sleep=3", "--tries=3", "--timeout=120"]
    depends_on:
      app:
        condition: service_started
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy

  scheduler:
    image: ${APP_IMAGE:-dycrm-local-app:latest}
    restart: unless-stopped
    working_dir: /var/www/html
    env_file:
      - .env.local
    volumes:
      - .:/var/www/html
      - vendor:/var/www/html/vendor
      - storage:/var/www/html/storage
      - bootstrap-cache:/var/www/html/bootstrap/cache
    command: ["sh", "-c", "while true; do php artisan schedule:run --verbose --no-interaction; sleep 60; done"]
    depends_on:
      app:
        condition: service_started
      mysql:
        condition: service_healthy
      redis:
        condition: service_healthy

volumes:
  mysql-data:
  redis-data:
  storage:
  bootstrap-cache:
  vendor:
